// This function prints in screen whether the checksum is correct or invalid and outputs the checksum value

#ifndef CHECKSUM_H 
#define CHECKSUM_H 

#include <stdio.h>
#include <stdbool.h>

// Function prototype
int checksum(int digits, unsigned long long n);

#endif // CHECKSUM_H